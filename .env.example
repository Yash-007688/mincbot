HOST=127.0.0.1
PORT=25565
# Set to a specific protocol version string if your server requires it (e.g. 1.20.4); otherwise leave empty
VERSION=
BOT_COUNT=20
BASE_NAME=PvPBot_
ATTACK_RANGE=16
RETARGET_MS=2500
LOG_PREFIX=[PvP]
# Comma-separated list of usernames to never attack
EXCLUDE_NAMES=

